<analysis>**original_problem_statement:**
The user wants to build a tracking application similar to Hyros.

**PRODUCT REQUIREMENTS:**
- A  script to capture form data (, , ), session data (URLs, referrers, UTMs), and a unique .
- A backend to store this data, stitch sessions from iframes (like StealthWebinar), and provide APIs for data retrieval and management.
- A frontend dashboard to display contacts, with a modal to view detailed information including all visited URLs and attribution data.
- The ability to delete contacts (single and bulk).
- A flexible, Zapier-style automation builder to send lead data to webhooks based on triggers, filters, and conditional logic (e.g., required fields, delays).
- A sales tracking page to ingest sales data from a webhook and link it to contacts.
- A dedicated page for stealth contacts ingested from a separate webhook (e.g., from StealthWebinar), which should be differentiated from script-tracked contacts.
- A simple password-based authentication system for the dashboard.
- A premium, modern, and highly responsive user interface with specific brand colors (, ) and a text logo TETHER.

**User's preferred language**: English

**what currently exists?**
A sophisticated full-stack application named Tether with a FastAPI backend and a React/Tailwind/Shadcn frontend, using MongoDB as the database.
- **Tracking & Data:** A  script captures comprehensive user data. The backend handles event tracking, advanced session/IP stitching, and data storage.
- **Frontend UI:** The UI has been extensively revamped with a light theme, brand colors, and improved layouts. It includes pages for Analytics, Visitors, Leads (identified contacts), Stealth (webhook-sourced contacts), Sales, and Automations. Data fetching is managed by  for performance and to prevent stale data issues.
- **Modals:** A highly polished and consistent modal system is used for viewing contact, sale, and run details, featuring smooth animations via Framer Motion.
- **Automations (Legacy):** An initial version of an automation system exists, allowing webhooks to be fired based on filters, required fields, and delays.
- **Authentication:** A simple token-based login page protects the application.

**Last working item**:
- **Last item agent was working on**: A complete overhaul of the automation builder. The user found the existing step-based builder too rigid and requested a full-page, flexible, Zapier-style builder where different action types (Wait For, Filter, Delay, Webhook) can be added, removed, and reordered freely.
- **Status**: IN PROGRESS
- **Agent Testing Done**: N
- **Which testing method agent to use?**: both
    - After the feature is complete, the frontend testing agent should be used to verify the new builder's UI, including adding/removing/reordering steps.
    - The backend testing agent should be used to verify the new  logic handles all step types and sequences correctly.
- **User Testing Done**: N

**All Pending/In progress Issue list**:
- **Issue 1:** The user wants a more flexible, Zapier-style automation builder. This is the highest priority task and is currently in progress. The current implementation is a complete refactor.

**Issues Detail:**
- **Issue 1: Complete the new Automation Builder.**
    - **Attempted fixes:** The agent has laid the backend groundwork. The  model now includes a  field. A new  function has been created in  to process these steps sequentially. The backend is designed to be backward-compatible, executing the old / logic if the new  field is not present on an automation.
    - **Next debug checklist:** The next step is to build the new full-page frontend for the builder. This will involve creating a new component (e.g., ) that allows the user to dynamically add, configure, reorder, and remove steps of different types (Wait For, Filter, Delay, Send Webhook).
    - **Why fix this issue and what will be achieved with the fix?** To provide the flexible and powerful automation system the user requires, moving beyond the initial rigid implementation.
    - **Status**: IN PROGRESS
    - **Is recurring issue?** N
    - **Should Test frontend/backend/both after fix?** both
    - **Blocked on other issue:** None

**In progress Task List**:
- **Task 1: Build the Zapier-style Automation Builder Frontend (P0)**
    - **Where to resume**: Create the new full-page React component for the automation builder. This component should:
        1.  Fetch the automation details.
        2.  Render a UI that allows adding/removing/reordering steps (, , , ).
        3.  Allow configuration of each step (e.g., setting delay time, defining filter rules).
        4.  On save, send the new  array to the  endpoint.
    - **What will be achieved with this?** A user-friendly and highly flexible automation builder that meets the user's Zapier-like requirement.
    - **Status**: IN PROGRESS
    - **Should Test frontend/backend/both after fix?** both
    - **Blocked on something**: No

**Upcoming and Future Tasks**
- None. The current focus is entirely on completing the new automation builder.

**Completed work in this session**
- **Core Functionality:**
    - Full-stack Hyros-like tracking application (FastAPI + React + MongoDB).
    - Advanced iframe session stitching and IP-based contact merging.
    - Implemented a robust data model for contacts, events, attribution, and sales.
    - Created single and bulk contact deletion functionality.
- **UI/UX & Frontend:**
    - Migrated all server state management to , eliminating stale data flashes and simplifying data fetching logic.
    - Implemented a major UI revamp to a premium, light-themed design based on user-provided assets and brand colors.
    - Built a consistent navigation structure with a top navigation bar and pages for Analytics, Visitors, Leads, Stealth, Sales, and Automations.
    - Replaced all default CSS animations in the Dialog component with smooth Framer Motion transitions, fixing overlay flashing issues.
    - Fixed all table column alignment issues for a cleaner look.
- **Features & Enhancements:**
    - Built a simple, secure password-based authentication system.
    - Created a  page and webhook to ingest and display confirmed registrations from external sources like StealthWebinar, automatically tagging them.
    - Implemented a feature-rich automation run history viewer with full-page view, date/type filtering, custom date ranges with a calendar picker, and CSV export.
    - Added timezone awareness across the entire application, with a user-selectable preference stored in .
    - Added PDF export for leads, with logic to handle large datasets by sending IDs in a POST body to avoid URL length limits.
    - Enhanced the (now legacy) automation system with required fields and delay + refetch capabilities.
- **Production & Deployment:**
    - Created a comprehensive production deployment guide and a  script using .
    - Diagnosed and fixed critical production bugs, including a database race condition () and an N+1 query issue that caused API timeouts with large datasets.

**Earlier issues found/mentioned but not fixed**
- None. The agent has been diligent in addressing all identified issues.

**Known issue recurrence from previous fork**
- None.

**Code Architecture**


**Key Technical Concepts**
- **Backend**: FastAPI, Pydantic, Motor (async MongoDB), BackgroundTasks for asynchronous operations.
- **Frontend**: React,  for server state, Framer Motion for animations, TailwindCSS, Shadcn/UI.
- **Database**: MongoDB with async access.
- **Authentication**: JWT-based, with credentials stored in environment variables.
- **Architecture**:
    - **Event-Driven Automation:** Automations are triggered by API events (), not polling.
    - **Backward Compatibility:** The new automation system is designed to run alongside the old one by checking for the presence of the new  field.
    - **State Management:** All server state (contacts, sales, logs, etc.) is managed by TanStack Query, while local UI state uses React's .
    - **Component Keying:** The  pattern on  is used to force component remounts and prevent stale state, a critical fix from this session.

**key DB schema**
- **contacts**: 
- **page_visits**: 
- **stealth_registrations**: 
- **sales**: 
- **automations**: 
- **automation_runs**: 

**changes in tech stack**
- Introduced  for all server state management.
- Introduced  for UI animations.
- Introduced  and  for PDF generation.

**All files of reference**
- : Contains all API logic, including the new automation step pipeline executor.
- : Main application shell, handles routing and global providers ().
- : The legacy automation builder, which will be replaced by a new full-page component.
- : A key component, refactored to use an inner keyed component () to prevent UI flashes.
- : Example of a data-heavy page using TanStack Query and featuring the new PDF export functionality.
- : Modified to use Framer Motion for smooth animations instead of default CSS animations.

**Areas that need refactoring**:
- The  component is now legacy code and should be removed or refactored into the new full-page builder once it's complete.

**key api endpoints**
- : Captures lead data from the script and triggers automations.
- : Ingests confirmed registrations and tags them.
- : Ingests sales data.
- : Fetches all contacts for the Visitors/Leads pages.
- : Fetches all automations.
- : Updates an automation (will be used to save the new  array).
- : Authenticates the user.

**Critical Info for New Agent**
- The user's top priority is the new flexible, Zapier-style automation builder. You must build the full-page frontend for this.
- The backend has been updated to support a  array in the  collection. New automations should be saved with this structure. The backend will fall back to the old logic for existing automations without the  field, ensuring no breakage.
- All data fetching in the frontend now uses . Do not use manual  inside  with . Use  for fetching and  with  for updates.
- The modal flashing issue was solved by splitting  into a static shell and a keyed inner component (). Follow this pattern for any similar issues.

**documents and test reports created in this job**
- 
[1m[0;36m=========================================[0m
[1m[0;36m  Tether — Production Deploy[0m
[1m[0;36m  2026-03-11 13:04:43[0m
[1m[0;36m=========================================[0m

[0;36m[INFO][0m  Running pre-flight checks…
[0;31m[ERROR][0m pm2 not found  — install with: sudo npm i -g pm2: Production deployment script.
- : PM2 configuration for production deployment.

**Last 10 User Messages and any pending HUMAN messages**
1.  **User:** My automations aren't firing on production.
    *   *Agent diagnosed it was due to the required fields gate waiting for a phone number that wasn't being provided.*
2.  **User:** That's not true, the form collects phone.
    *   *Agent confirmed the required fields logic was working as intended and the issue was data-specific.*
3.  **User:** In the runs view for a multi-step automation, I can't tell which step fired.
    *   *Agent added a webhook destination badge to each run row and added action details to the CSV export.*
4.  **User:** I need a new page  for contacts from a webhook, and it should show if they have a .
    *   *Agent built the  page and the corresponding backend webhook.*
5.  **User:** How can I differentiate between stealth contacts and tracked contacts in automations? And can I add a wait time?
    *   *Agent implemented automatic  tagging and added a Delay + Refetch capability to automation actions.*
6.  **User:** I can't see the Stealth page in the navbar.
    *   *Agent fixed the missing navigation tab.*
7.  **User:** I don't like the current automation builder. It's not flexible. I want a full-page, Zapier-style builder where I can choose my steps.
    *   *Agent acknowledged and started planning the major refactor.* (This is the current task).
8.  **User:** Do a code review for the new changes and make sure it's production-ready.
    *   *Agent performed a thorough review and fixed several bugs related to the multi-action automation changes.*
9.  **User:** Does the automation fire again if a contact re-opts in?
    *   *Agent explained the current deduplication logic and its limitations.*
10. **User:** Add phone and payload to the automation export CSV.
    *   *Agent implemented this change.*

**Project Health Check:**
- **Functional**: The application is functional with a rich feature set.
- **Broken**: The new automation builder is mid-implementation.
- **Mocked**: No parts of the application are mocked.

**3rd Party Integrations**
- None.

**Testing status**
- **Testing agent used after significant changes**: NO
- **Troubleshoot agent used after agent stuck in loop**: NO
- **Test files created**: None.
- **Known regressions**: None.

**Credentials to test flow:**
- The login credentials are set in  as  and .

**What agent forgot to execute**
- The agent was in the middle of a major refactor of the automation builder when the session ended. The task is not forgotten, but it is incomplete and is the highest priority for the next agent.</analysis>
